pub fn test_one() {
    let x = i32::from(5);
    let y = x;
    print!("x 的值为: {}，y 的值为: {}\n", x, y)
    // 修改 y 的值

    // let s1 = String::from("hello");

    // // 只有一个引用指向 s1
    // let s2 = &s1;

    // println!("{}, world!", s1);
    // print!("{}", s2)
}
